# Optional: enables LLM auto-scoring on the per-area scoring form.
# Without it, the app works fully with manual-only scoring (auto-score
# silently returns nothing - no error is shown).
GROQ_API_KEY=
